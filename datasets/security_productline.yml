unique_name: security_productline
object_type: dataset
label: security_productline
columns:
  - name: PRODUCTLINE
    data_type: string
  - name: USER_ID
    data_type: string
connection_id: common_connection
sql: |-
  select
  'R' as productline,
  'marco.muller@atscale-se-demo.com' as user_id
